if (project.hasProperty("optifineMcJarPath") && project.optifineMcJarPath.toString().length() > 0) {
    minecraft {
        runs {
            client {
                property 'ofdev.mcjar', project.optifineMcJarPath
            }
        }
    }
}